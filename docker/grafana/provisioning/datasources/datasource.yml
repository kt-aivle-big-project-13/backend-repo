apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    # 프로비저닝된 대시보드 JSON이 이 uid 로 데이터소스를 참조한다.
    # 값을 바꾸면 대시보드의 데이터소스 연결이 끊어진다.
    uid: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true